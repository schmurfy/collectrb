This very simple Ruby class sends monitoring statistics to a collectd server
using the collectd UDP protocol. It supports gauges, counters, derive, and
absolute. It does not (yet) support other types.
